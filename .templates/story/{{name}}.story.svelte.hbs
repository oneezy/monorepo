<script>
	import { hstEvent } from 'histoire/client'
  import {{name}} from '@packages/components/src/{{name}}/{{name}}.svelte';

	export let Hst
	const source = `
  <{{name}}>
    {{name}}
  </{{name}}>
`

	let disabled = false
	const hstEventClick = event => hstEvent('click', event)
</script>

<Hst.Story title="{{name}}" {source}>
  
	<{{name}} on:click={hstEventClick}>
		{{name}}
	</{{name}}>

	<svelte:fragment slot="controls">
		<Hst.Checkbox bind:value={disabled} title="Disabled" />
		<pre>{JSON.stringify({ disabled }, null, 2)}</pre>
	</svelte:fragment>
</Hst.Story>
